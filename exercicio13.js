/**
 * Crie um programa que inverta a ordem dos elementos em uma lista.
 */

const lista = [1, 2, 3, 4, 5, 6];

console.log(`Lista inicial: ${lista}`)

lista.reverse();

console.log(`Lista invertida: ${lista}`);